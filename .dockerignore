# Reduz o contexto de build e — crítico — mantém segredos fora da imagem.
# Sem isto o `next build` carregaria .env.local e embutiria/vazaria segredos.
node_modules
.next
.git
.env
.env.*
test-results
tests
playwright.config.ts
*.png
*.log
.lina
docs
tasks
scratchpad
.vercel
# pnpm-store local (cache do pnpm) — 1.3GB+ que só inflava o contexto de
# build, achado ao vivo num build que demorou o triplo do normal.
.pnpm-store
